public class MonthlyDataBackupJob implements Schedulable {
    public void execute(SchedulableContext sc) {
        // Logic for monthly data backup
        System.debug('Monthly data backup job is running.');
        // Add your data backup logic here
    }
}