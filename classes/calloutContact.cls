public class calloutContact {
    public static HttpResponse calloutContact(HttpRequest requestSent) {
        //Get response
        Http http=new Http();
		HttpResponse response=http.send(requestSent);
				
		return Response;
    }
}